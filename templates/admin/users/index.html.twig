{% extends 'base.html.twig' %}

{% block title %}Utilisateurs - Administration - Linott{% endblock %}

{% block body %}
{% include 'components/_navbar.html.twig' %}

<div class="container mx-auto px-4 py-6">
    {% include 'components/_flash_messages.html.twig' %}

    <div class="flex items-center justify-between mb-6">
        <div>
            <a href="{{ path('app_admin') }}" class="text-sm text-gray-500 hover:text-gray-700">&larr; Administration</a>
            <h1 class="text-2xl font-bold text-gray-900">Utilisateurs</h1>
        </div>
        <a href="{{ path('app_admin_users_new') }}"
           class="bg-primary-600 text-white px-4 py-2 rounded-lg hover:bg-primary-700 transition-colors">
            + Nouvel utilisateur
        </a>
    </div>

    <div class="bg-white rounded-lg shadow overflow-hidden">
        <table class="w-full">
            <thead class="bg-gray-50">
                <tr>
                    <th class="px-4 py-3 text-left text-sm font-medium text-gray-500">Nom</th>
                    <th class="px-4 py-3 text-left text-sm font-medium text-gray-500">Email</th>
                    <th class="px-4 py-3 text-left text-sm font-medium text-gray-500">Rôles</th>
                    <th class="px-4 py-3 text-center text-sm font-medium text-gray-500">Statut</th>
                    <th class="px-4 py-3 text-right text-sm font-medium text-gray-500">Actions</th>
                </tr>
            </thead>
            <tbody class="divide-y divide-gray-200">
                {% for user in users %}
                    <tr class="hover:bg-gray-50">
                        <td class="px-4 py-3">
                            <span class="font-medium text-gray-900">{{ user.fullName }}</span>
                        </td>
                        <td class="px-4 py-3 text-gray-600">{{ user.email }}</td>
                        <td class="px-4 py-3">
                            {% for role in user.roles %}
                                <span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium {% if role == 'ROLE_ADMIN' %}bg-red-100 text-red-800{% else %}bg-gray-100 text-gray-800{% endif %}">
                                    {{ role|replace({'ROLE_': ''}) }}
                                </span>
                            {% endfor %}
                        </td>
                        <td class="px-4 py-3 text-center">
                            {% if user.actif %}
                                <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                    Actif
                                </span>
                            {% else %}
                                <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-800">
                                    Inactif
                                </span>
                            {% endif %}
                        </td>
                        <td class="px-4 py-3 text-right">
                            <div class="flex items-center justify-end gap-2">
                                <a href="{{ path('app_admin_users_edit', {id: user.id}) }}"
                                   class="text-primary-600 hover:text-primary-700 text-sm font-medium">
                                    Modifier
                                </a>
                                <form action="{{ path('app_admin_users_toggle', {id: user.id}) }}"
                                      method="post"
                                      class="inline">
                                    <input type="hidden" name="_token" value="{{ csrf_token('toggle' ~ user.id) }}">
                                    <button type="submit"
                                            class="text-amber-600 hover:text-amber-700 text-sm font-medium">
                                        {% if user.actif %}Désactiver{% else %}Activer{% endif %}
                                    </button>
                                </form>
                            </div>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="5" class="px-4 py-8 text-center text-gray-500">
                            Aucun utilisateur
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endblock %}
